pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps { echo "step 2" }
        }
        stage('Build & Test') {
            steps { echo "step 3" }
        }
        stage('Deploy') {
            steps { echo "step 2" }
        }
    }
}
